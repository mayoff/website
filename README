

This is the repository for my blog.  The "source code" is under the src directory.  The published files are under the ship directory, which has its own (detached) repository in the .ship-git directory.


